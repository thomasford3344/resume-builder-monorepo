# Backend API base URL (optional in local dev — Vite proxies /api to localhost:3000)
VITE_API_BASE_URL=

# Socket.io server URL (falls back to VITE_API_BASE_URL, then http://localhost:3000)
VITE_SOCKET_URL=
